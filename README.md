**Description:**

> Built a Random Forest model on the Titanic dataset to predict passenger survival based on features such as age, sex, passenger class, and fare. The model was trained on historical passenger data and evaluated using validation accuracy to measure its prediction performance.
